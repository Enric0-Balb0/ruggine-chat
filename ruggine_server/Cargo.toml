[package]
name = "ruggine_server"
version = "0.1.0"
edition = "2021"

[lib]
name = "ruggine_server"
path = "src/lib.rs"

[dependencies]
axum = { version = "0.7", features = ["macros"] }
headers = "0.4"
hyper = { version = "1.0", features = ["http1", "http2", "server", "client"] }
hyper-util = { version = "0.1", features = ["tokio"] }
tokio = { version = "1.24.2", features = ["macros", "rt-multi-thread", "time"] }
tower = { version = "0.4.13", features = ["util"] }
tower-http = { version = "0.5", features = ["cors", "trace"] }
dotenv = "0.15.0"
serde =  { version = "1.0.152", features = ["derive"] }
jsonwebtoken = "8.3.0"
thiserror = "1.0.40"
chrono = {version = "0.4.24", features = ["serde"]}
bcrypt = "0.14.0"
serde_json = "1.0.95"
mockall = "0.11.4"
sqlx = { version = "0.6.3", default-features = false, features = [
  "runtime-tokio-native-tls",
  "mysql",          # o "mysql" o "sqlite"
  "chrono",
  "migrate",
  "macros",
  "time"
] }
validator = { version = "0.16.0", features = ["derive"] }
async-trait = "0.1.68"
tracing-subscriber = "0.3.17"
once_cell = "1.19"
rand = "0.8"

[dev-dependencies]
# Test-specific dependencies
tokio-test = "0.4"
futures = "0.3"
env_logger = "0.10"
serial_test = "3.0"  # For tests that cannot run in parallel
tempfile = "3.5"    # For temporary files in tests

# [[test]]
# name = "user_repository_unit"
# path = "tests/unit/repository/user_repository_test.rs"

# [[test]]
# name = "user_dto_unit"
# path = "tests/unit/dto/user_dto_test.rs"

# [[test]]
# name = "user_repository_integration"
# path = "tests/integration/repository/user_repository_test.rs"

# Ottimizzazioni di compilazione
[profile.dev]
debug = true
# incremental = true
opt-level = 0

[profile.test]
debug = true
# incremental = true
opt-level = 0

# Ottimizza le dipendenze per velocizzare i build
# [profile.dev.package."*"]
# opt-level = 1
# debug = false

# [profile.test.package."*"]
# opt-level = 1
# debug = false

# [alias]
# llvm-cov_ruggine_server = "llvm-cov --html --output-dir coverage --remap-path-prefix"