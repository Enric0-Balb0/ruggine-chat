# PostgreSQL configuration for Ruggine production deployment
# Optimized for small to medium workloads

# Connection Settings
max_connections = 100
listen_addresses = '*'

# Memory Settings (adjust based on available RAM)
shared_buffers = 256MB
effective_cache_size = 1GB
work_mem = 4MB
maintenance_work_mem = 64MB

# Checkpoint Settings
checkpoint_completion_target = 0.9
wal_buffers = 16MB
default_statistics_target = 100

# Logging
log_destination = 'stderr'
logging_collector = on
log_directory = 'pg_log'
log_filename = 'postgresql-%Y-%m-%d_%H%M%S.log'
log_truncate_on_rotation = on
log_rotation_age = 1d
log_rotation_size = 10MB

# Log what we care about for monitoring
log_min_duration_statement = 1000  # Log slow queries (1 second)
log_connections = on
log_disconnections = on
log_lock_waits = on
log_statement = 'none'

# Performance
random_page_cost = 1.1  # Assuming SSD storage
effective_io_concurrency = 200

# Security
ssl = off  # Enable if you have SSL certificates
password_encryption = md5

# Locale
lc_messages = 'en_US.utf8'
lc_monetary = 'en_US.utf8'
lc_numeric = 'en_US.utf8'
lc_time = 'en_US.utf8'
default_text_search_config = 'pg_catalog.english'

# Timezone
timezone = 'UTC'

# Shared preload libraries
shared_preload_libraries = ''

# Enable query plan statistics
track_activities = on
track_counts = on
track_io_timing = on
track_functions = pl
